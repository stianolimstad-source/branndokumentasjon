## Endring

I `src/pages/Konsept.tsx` (linje 9975–9982): Pakk `kjoreveiKrav`-textarean ("Krav til utforming av kjørevei for kjøretøy") inn i `{!isBF85Tilstand && ( ... )}` slik at feltet skjules for BF85-tilstandsvurderinger.

## Avgrensning

- Kun UI skjules. State/defaults og forhåndsvisning/Word-eksport berøres ikke.
- TEK17-konsept: uendret.
